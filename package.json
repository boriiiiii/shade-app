{
  "name": "shade",
  "version": "0.1.0",
  "private": true,
  "description": "Shade — bot Solana copytrade/sniping avec interface façon terminal (démo étudiant)",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../mobile && npm install",
    "dev": "concurrently -n backend,mobile -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix mobile start\"",
    "backend": "npm --prefix backend run dev",
    "mobile": "npm --prefix mobile start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
